version: '3'
services:
  app:
    build: ../path/to/Dockerfile  # Adjust the path as needed
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    env_file:
      - ../config/dev.env
    ports:
      - "8080:8080"

