version: '3'
services:
  app:
    build: ../path/to/Dockerfile  # Adjust the path as needed
    environment:
      - SPRING_PROFILES_ACTIVE=systest
    env_file:
      - ../config/systest.env
    ports:
      - "8080:8080"
